<div class="container"> 
<hr>
    <img class='team-img' src="<%= @team.image %>" alt="<%@team.name%>"> 
    <h1 class="text-center mt-4"><%= @team.name %></h1> 
    <h5 class="text-center mt-4"><%= @team.city %></h5>  
    
    
    <h2>All games played by this team</h2> 
    <% @team.games.uniq.each do |game| %> 
        <p class="center"><%= link_to game.name, game_path(game) %></p>
    <% end %> 
    <h2><%= button_to "Back", teams_path, method: :get %></h2> 
</div>